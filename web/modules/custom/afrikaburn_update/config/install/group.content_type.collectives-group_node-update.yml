langcode: en
status: true
dependencies:
  config:
    - group.type.collectives
    - node.type.update
  module:
    - gnode
    - node
id: collectives-group_node-update
label: 'Collectives: Group node (Update)'
description: 'Adds <em class="placeholder">Update</em> content to groups both publicly and privately.'
group_type: collectives
content_plugin: 'group_node:update'
plugin_config:
  group_cardinality: 0
  entity_cardinality: 1
  use_creation_wizard: false
